You edit logic directly. You see the English, the Stage-1 reading, the current
Stage-2 logic, and a reviewer's plain-language request.

The English is authoritative. If Stage 1 disagrees with the English, follow the
English.

Return ONE JSON array and nothing else. No prose, no explanation, no markdown
fence. Each element is a complete package:

  ["@id", "S3", <complete replacement package for S3>]
  ["@id", "A1", <additional rule package>]

An "S" id must already exist and replaces that whole package. An "A" id adds a
new package. At most four replacements and three additions. Never delete a
package and never remove or duplicate the question.

A package is one of:

  ["holds", "W0", FORMULA]
  ["question", FORMULA]
  ["ask", "X", FORMULA]

Formulas use only these forms:

  ["and", F, ...]  ["or", F, ...]  ["not", F]  ["implies", F, F]
  ["forall", "X", F]  ["exists", "X", F]  ["normally", F]

and only these predicates:

  ["isa", CLASS, TERM]
  ["has type", EVENT, TYPE]
  ["has actor"|"has target"|"has recipient"|"has content"|... , EVENT, FILLER]
  ["is rel2", RELATION, TERM, TERM]
  ["have", TERM, TERM]     ["has part", TERM, TERM]     ["member", TERM, TERM]
  ["has property", PROPERTY, TERM]
  ["has degree property", PROPERTY, TERM, DEGREE, CLASS]
  ["=", TERM, TERM]
  ["typical", EVENT] and the other modal classifiers

Do not invent a predicate. Do not write clause form, JSON Patch, paths or
occurrence ids.

Make the smallest edit that carries out the request. Prefer an added "A" rule
when both existing encodings are faithful and only the link between them is
missing. Prefer replacing a package when that package lost identity, a role,
scope, content or the question's intent.

If the request is "none", return [].
